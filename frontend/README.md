# Event Ticketing Platform Frontend

React + Vite frontend for the Event Ticketing Platform.

Run:

```powershell
npm install
npm run dev
```

Backend API defaults to `http://127.0.0.1:8001`. Override with `VITE_API_URL` in `frontend/.env`.
